# 实测记录

用合成数据对 Jev 1.13 做的几轮测试。结论和踩的坑都记在这里。
**所有数字来自人工构造的中文任务，不能外推到其他领域。**

## 1. confidence 是不是摆设

`experiments/calib2.py`：160 条程序化生成的样本（ground truth 由算术唯一确定，
不依赖人工标注），分 choice / noul 两类、四个难度档。

```
总体准确率 95.0%
ECE  全部 0.0346   choice 0.0556   noul 0.0135
Brier 0.0451
```

可靠性曲线的**所有偏差都是负的**——模型自报的概率系统性地低于实际准确率：

```
区间            n    实际准确率   模型说的概率     偏差
[0.50,0.60)     8      62.5%      0.551      -0.074
[0.70,0.80)    12      83.3%      0.763      -0.070
[0.80,0.90)    16      93.8%      0.852      -0.086
[0.90,0.99)    66      97.0%      0.960      -0.010
[0.99,1.01)    53     100.0%      0.994      -0.006
```

ECE < 0.05 算校准良好，LLM 自报置信度通常在 0.15~0.30。偏保守是安全的方向：
按它说的定阈值只会比预期更稳。

阈值表（choice）：`conf >= 0.80` 时 65% 的量可全自动且零错误，再往上加阈值
只是多转人工，不再降低错误。

**但高 confidence 不等于保证**：两个样本在 conf 0.97 / 0.95 上判错。校准好的含义
是「说 95% 就约 95% 对」——剩下 5% 是真的会发生。

## 2. 最重要的坑：题目无解时，它自信地错

第一版测试跑出来准确率只有 85%，choice 的 tier-1（最简单档）只有 55%。
查下来是**我自己的 bug**：套餐价格表只存在于 Python 代码里，从没放进 `state`。
模型无从知道「旗舰版 = 899 元」，只能从字面语感加席位数去猜。

题目对它是**无解的**，但它照样给出了 conf 0.85、0.90 的答案。修掉之后
准确率 85% → 95%，ECE 0.054 → 0.035。

> **confidence 衡量的是「在给定信息下答案分布有多集中」，不是「这题我有没有能力答」。
> 它没法告诉你「你少给了价格表」。**

上线后真正会咬人的不是模型能力边界，是自己 state 里漏了字段。只能靠代码保证输入完整性。

## 3. Noul 还是 Choice

`experiments/noul_vs_choice.py`：同一批 80 道题，两种问法各跑一遍。
Choice 是四选一；Noul 拆成 N 个「X 是不是最高的？」取概率最大者。

```
                Choice      Noul 拆解
准确率           93.8%        91.2%
ECE             0.0645       0.1108
input tokens     40209        44609
分歧 4 题        choice 对 3   noul 对 1

同样做到 100% 准确：Choice conf>=0.8 覆盖 62.5%，Noul margin>=0.7 只覆盖 42.5%
```

原因是拆成独立 Noul 丢掉了互斥约束。N 个 noul 的概率之和实测 min 0.30 / median 1.07
/ max 1.69，而 Choice 恒等于 1.00。**那个归一化本身就是信息量。**

判断方法：**「这几个答案能同时为真吗？」** 不能 → Choice；能 → 每个标签一个 Noul。

（早先我根据不同任务上的 ECE 得出过「Noul 更可信」的结论，那是错的——两组题目
难度不可比，不构成证据。同题对照才算数。）

## 4. 要不要重复采样

`experiments/determinism.py`：同一请求打 10 次。

**不是确定性的**，概率会抖（choice conf 在 0.69~0.77 之间浮动，某条 noul 在
0.04~0.22 之间跳），**但 argmax 完全稳定**，10/10 给出同一个答案。
官方 cookbook 报的每题概率标准差 σ = 0.0102。

所以重复采样投票**大部分时候是白花钱**。唯一有意义的场景是概率贴近决策阈值时——
而那种情况更该做的是**把 0.30~0.70 整段划为不自动决策区**，用区间吸收抖动，
比重采样便宜也更可靠。这也是官方的建议。

## 5. 性能和成本

```
160 条并发   4.1s   39 req/s   单请求 p50 约 1.1s（主要是跨境 RTT）
整轮 input tokens 71,714 ≈ $0.003
```

单请求延迟基本全是网络往返，模型推理时间淹没在里面。并发几乎线性叠加，
实际用法应该是批量 fan-out。

## 环境

- `jev-1.13.0`（`jev-latest` 当前指向它）
- typesafe-sdk 0.7.1
- **上线请固定版本号**，阈值是和模型版本绑定的
